# DO NOT EDIT THIS FILE BY HAND!!
# It has been autogenerated from crates.d/rsp2-tasks.Cargo.toml
#
# To update it, run './crates gen' in the workspace root

[package]
name = "rsp2-tasks"
version = "0.1.0"
authors = ["Michael Lamparski <diagonaldevice@gmail.com>"]
description = "Where madness reigns eternal."

# Note: Many files within this crate are dual-licensed under MIT and Apache 2.0
license = "GPL-3.0"

[lib]
path = "lib.rs"

[dependencies]
rsp2-tasks-config = { path = "config" }

rsp2-lammps-wrap = { path = "../io/lammps" }
rsp2-minimize = { path = "../minimize" }
rsp2-structure = { path = "../structure" }
rsp2-structure-io = { path = "../io/structure" }
rsp2-phonopy-io = { path = "../io/phonopy" }
rsp2-array-utils = { path = "../util/array" }
rsp2-array-types = { path = "../util/array-types" }
rsp2-soa-ops = { path = "../util/soa-ops" }
rsp2-slice-math = { path = "../util/slice-math" }
rsp2-util-macros = { path = "../util/macros" }
rsp2-fs-util = { path = "../util/fs" }
rsp2-clap = { path = "../util/clap" }
rsp2-assert-close = { path = "../util/assert-close" }
rsp2-linalg = { path = "../linalg" }
rsp2-newtype-indices = { path = "../util/newtype-indices" }

clap = "2"
slice-of-array = "0.2"
rsp2-kets = { version = "0.3.6", git = "https://github.com/ExpHP/rsp2-kets", features = ["serde"] } 
rand = "0.3"
log = "0.4"
env_logger = "0.5.10"
failure = "0.1"
fern = { version = "0.5", features = ["colored"] } 
extension-trait = "0.1.2"
itertools = "0.7"
lazy_static = "0.2"
ansi_term = "0.10"
rayon = "1.0.1"
serde = { version = "1.0.63", features = ["rc"] } 
serde_derive = "1.0.47"
serde_json = "1"
serde_yaml = "0.7"
serde_ignored = "0.0.4"
path_abs = "0.4.1"
enum-map = "0.2.24"
frunk = "0.2.0"
shlex = "0.1.1"
indoc = "0.2.3"
lapacke = "0.1.4"
lapack-src = { version = "0.1.4", features = ["netlib"] } 
num-traits = "0.2.3"
num_cpus = "1.8"

# FIXME: should be an optional dependency, but the crates script
#        has no way to do that.
mpi = "0.5.4"

[features]
default = ["_mpi"]

nightly = ["beta", "rsp2-array-types/nightly", "rsp2-array-utils/nightly", "rsp2-assert-close/nightly", "rsp2-clap/nightly", "rsp2-fs-util/nightly", "rsp2-lammps-wrap/nightly", "rsp2-linalg/nightly", "rsp2-minimize/nightly", "rsp2-newtype-indices/nightly", "rsp2-phonopy-io/nightly", "rsp2-slice-math/nightly", "rsp2-soa-ops/nightly", "rsp2-structure-io/nightly", "rsp2-structure/nightly", "rsp2-tasks-config/nightly", "rsp2-util-macros/nightly"]
beta = ["rsp2-array-types/beta", "rsp2-array-utils/beta", "rsp2-assert-close/beta", "rsp2-clap/beta", "rsp2-fs-util/beta", "rsp2-lammps-wrap/beta", "rsp2-linalg/beta", "rsp2-minimize/beta", "rsp2-newtype-indices/beta", "rsp2-phonopy-io/beta", "rsp2-slice-math/beta", "rsp2-soa-ops/beta", "rsp2-structure-io/beta", "rsp2-structure/beta", "rsp2-tasks-config/beta", "rsp2-util-macros/beta"]

# FIXME: Rename feature to "mpi" once mpi is properly an optional dep.
#
#        This dep with the underscored name exists to make it possible to disable mpi-related
#        features (to test conditional compilation) despite the current inadequacies
#        of the crates script
_mpi = ["rsp2-lammps-wrap/_mpi"]

