# DO NOT EDIT THIS FILE BY HAND!!
# It has been autogenerated from crates.d/rsp2-phonopy-io.Cargo.toml
#
# To update it, run './crates gen' in the workspace root

[package]
name = "rsp2-phonopy-io"
version = "0.1.0"
authors = ["Michael Lamparski <diagonaldevice@gmail.com>"]
description = "Parses/writes some of phonopy's filetypes.  And some of rsp2's own made-up filetypes used around phonopy. Not sure why this exists really. Maybe to save `rsp2-tasks` some compile time?"

[lib]
path = "lib.rs"

[dependencies]
failure = "0.1"
nom = "3"
serde = "1"
serde_derive = "1"
serde_yaml = "0.7"
serde_json = "1"
byte-tools = "0.2.0"

rsp2-structure = { path = "../structure" }
rsp2-array-types = { path = "../util/array-types" }

rsp2-kets = { version = "0.3.6", git = "https://github.com/ExpHP/rsp2-kets", features = ["serde"] } 

[features]
nightly = ["beta", "rsp2-array-types/nightly", "rsp2-structure/nightly"]
beta = ["rsp2-array-types/beta", "rsp2-structure/beta"]
