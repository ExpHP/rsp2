# NOTE: apply this after defaults.yaml

potential:
  - kc-z-new:
      cutoff: 14
      cutoff-length: 0 # simulate lammps' quirks
  - rebo-nonreactive:
      params: lammps

scale-ranges:
  scalables: []

parameters: null

phonons:
  eigensolver:
    dense: {}

bond-radius: 1.8
