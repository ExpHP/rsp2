# NOTE: apply this after defaults.yaml

potential:
  kc-z:
    cutoff: 14

scale-ranges:
  scalables: []

phonons:
  eigensolver:
    sparse: {}

cg.stop-condition: true # stop immediately

bond-radius: 1.8
lammps:
  processor-axis-mask: [true, true, false]

phonons.supercell.~~REPLACE~~:
  target: [20, 20, 0]
