# NOTE: apply this after defaults.yaml

potential:
  kc-z:
    cutoff: 14

scale-ranges:
  scalables: []

phonons:
  eigensolver:
    sparse: {}

bond-radius: 1.8
lammps:
  processor-axis-mask: [true, true, false]
