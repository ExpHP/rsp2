potential:
  kc-z:
     cutoff: 14

scale-ranges:
  scalables: []

parameters: null

phonons:
  disp-finder.~~REPLACE~~:
    rsp2: {}
  eigensolver.~~REPLACE~~:
    rsp2:
      dense: true

bond-radius: 1.8
lammps:
  processor-axis-mask: [true, true, false]
